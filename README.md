This is a website used to generate templates for a copying machine which works like a CNC machine but with a pen.
The objetive is to make it as easy, intuitive and customizable as possible for the user to specify the path of the pen along the paper.

TODO list:
- create some kind of warning to prevent users from using invalid characters
- create support for exporting files
- specify the strength and frequency (if applicable) of the noise to be added to the movement of the pen
- optimize rendering by using svg elements instead of manually rendering everything into the canvas
